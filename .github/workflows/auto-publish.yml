name: Auto News Publisher

on:
  schedule:
    # S'exécute 5 fois par jour, toutes les 4h en heures d'éveil (6h-22h,
    # fuseau Sénégal = UTC+0). Espacement volontaire pour garder une
    # régularité forte sans tomber dans la publication de masse qui nuit
    # à la confiance Google sur un site jeune sans autorité de domaine.
    - cron: '0 6,10,14,18,22 * * *'
  workflow_dispatch: # Permet de lancer le script manuellement depuis GitHub quand tu veux

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Installation des dépendances du script
        run: npm install rss-parser

      - name: Exécution du générateur d'articles
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node scripts/auto-publisher.mjs

      - name: Commit et Push de l'article généré
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add src/content/
          git diff --staged --quiet || (git commit -m "auto: publication quotidienne d'article [skip ci]" && git push)

